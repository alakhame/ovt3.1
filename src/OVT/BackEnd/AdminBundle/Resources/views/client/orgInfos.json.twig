{"id":{{org.id}}, "name":"{{org.name}}", "address":"{{org.address}}" , "phoneNumber":"{{org.phoneNumber}}" , "hashLink":"{{org.hashLink}}", "admins":[ {% for u in org.admins  %}
		{% if loop.last %} {"admin":"{{u.firstname ~' '~u.lastname}}"}
		{% else %}  {"admin":"{{u.firstname ~' '~u.lastname}}"},
		{% endif %}
	
	{% endfor %}

 ]}